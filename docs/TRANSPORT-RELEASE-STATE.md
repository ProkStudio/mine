# Transport release — рабочий конспект

Обновлено: 2026-09-07. Статус: работа начата, новый релиз НЕ опубликован.

## Контекст и ветка

- Запрос владельца: автономно довести транспортный пак до релиза; увеличить транспорт умеренно, добавить осмысленные анимации, устранить визуально висящие детали, улучшить звуки. Сохранять состояние работы на GitHub.
- Репозиторий: ProkStudio/mine. Продолжаем `feature/vehicle-art-pass-20260907`, PR #5.
- Исходный HEAD: `e548b459dbdde14a4468edb3539302ad45bc3878`. Main: `83df29ebd834ef50cf0a731bfe6bf7932b963b50`.
- Ветка уже включает необходимые исправления PR #3. Ветки оружия и отдельной механики комбайна не включать без проверки пересечений.
- Цель совместимости: Minecraft 1.21.11 / Fabric / Java 21; сохранить ID, формат состояния, груз и число мест.

## Подтверждено к началу работы

- Найдены 16 вариантов / 8 семейств, общая геометрия, UV-атласы, анимации пассажиров и 6 синтезированных звуковых профилей.
- README и PR фиксируют успешный CI на предыдущем коммите `83b0ad1`, НЕ на новых изменениях.
- Игровая приёмка прежней ветки не выполнена. Стабильного GitHub Release нет.
- В текущем sandbox есть Java 25 и Python; нет загруженных Gradle/Fabric-зависимостей и прямого DNS-доступа к github.com. Подключённый GitHub работает. Полную сборку выполнять через существующий CI; независимые классы и генераторы проверять локально.

## План / следующая точка входа

1. Прочитать актуальные `VehicleGeometry`, `VehicleDetailing`, `VehicleType`, `VehiclePresentation`, `CombineRenderer`, код аудио и генераторы; проверить существующие тесты и workflow.
2. Ввести согласованный умеренный масштаб: геометрия, места/точки контакта, эффекты и границы видимости; не увеличить только рендер отдельно от посадки.
3. Исправить опоры и шарниры; добавить механически связанные движения без отрыва деталей. Проверить весь парк офлайн.
4. Улучшить оригинальные звуки, переходы двигателя и лимиты голосов, проверить OGG и упаковку без сторонних нелицензированных записей.
5. Добавить регрессионные проверки, воспроизводимую сборку/упаковку, инструкции обновления и changelog.
6. Проверить актуальный CI, зафиксировать реальные результаты и ограничения. Не выдавать офлайн-рендеры за Minecraft и не заявлять непроведённую игровую приёмку.

## Правило продолжения

Перед изменениями перечитать этот файл, актуальный HEAD и изменяемые файлы. Обновлять этот конспект после каждого законченного блока. Не считать текущий план выполненной работой и не переносить старые PASS на новый коммит.

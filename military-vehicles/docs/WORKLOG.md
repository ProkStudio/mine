# Журнал разработки

## 2026-09-08 — S1 и прототип S2

- Найдена предыдущая точка продолжения: существовал предварительный план военной техники, но не код.
- По текущему запросу выбран существующий репозиторий mine вместо создания нового. Подготовлена отдельная ветка от `83df29e`; другие модули не менялись.
- Реализованы самостоятельная сборка, чистое ядро, строгий save v1, серверный грузовик 6×6, два места, канистра, ремонт, груз/подбор, клиентский ввод, модели и RU/EN.
- Автономный `tests/run-core.sh`: PASS, **90041 assertion**. Это не число JUnit-тестов и не игровых сценариев.
- Полная сборка `clean build` с `--offline --no-build-cache --rerun-tasks`: **PASS** на JDK 21, Gradle 9.2.0, Fabric Loom 1.14.10. Общий и клиентский код скомпилированы. **25 JUnit cases, 0 failures/errors/skips**; строгий verifier сверил обязательные методы и установочный remapped JAR.
- Исправлена выявленная Gradle зависимость `sourcesJar` от генератора ресурсов; после исправления повторно выполнен полный clean build.
- JAR: `military-vehicles-0.1.0-alpha.1.jar`, 181000 байт; SHA-256: `369c77ea1bb00c002e7a629b4a8a69227f1b96296d2e021f3f7364a32da45259`. Машинная сводка — BUILD-REPORT.json.
- Добавлен воспроизводимый генератор офлайн-страницы геометрии (`python tools/preview.py` после сборки). Изображения и игровая визуальная приёмка **не подтверждены просмотром**; наличие превью не считается PASS. Базовая геометрия проверена автоматическими ограничениями, но визуальный QA остаётся открытым.
- Minecraft-клиент, выделенный игровой сервер и сетевые сценарии не запускались.

Обнаруженные при проверке проблемы, исправления и финальный результат фиксируются ниже, а не заменяются предположением, что наличие исходников означает готовность.

## 2026-09-08 — продолжение S2: 0.1.0-alpha.2

- Прочитана ветка `feature/military-vehicles` и её точка продолжения; исходный коммит `3588a5a`. Два CI build этого исходного коммита имели success. Это результаты **alpha.1**, не новой итерации.
- Добавлены собственный mono OGG двигателя, независимые сглаженные петли, позиционный звук в радиусе 32 блоков, лимит 8 активных/затухающих петель и приоритет своей машины. Громкость подчиняется стандартным настройкам, потерянные при resource reload каналы могут перезапускаться.
- Временная нагрузка вычисляется сервером из проверенного ввода и сбрасывается при остановке двигателя; скорость звука основана на наблюдаемом горизонтальном перемещении, без телепортационных скачков. Сохранения v1, ID, груз и физика сохранены.
- Добавлен серверный дым у существующего торца трубы: 1 частица раз в 8/4 тика (холостой ход/нагрузка), с проверкой воздуха, жидкости, границы мира и загруженного чанка.
- Локально реально выполнены `CoreSmoke` — **90041 assertion**, `FeedbackSmoke` — **20487 assertions**. Новые исходники чистого ядра и генератор звука скомпилированы Java-компилятором с `--release 21`; установленная JVM — Corretto 25.0.4. Это не локальная Fabric-сборка и не JUnit.
- Генератор реально выполнил encode/decode через FFmpeg 7.0.2: mono 32000 Hz, 128000 samples, 34625 байт OGG; RMS 0.11947495, peak 0.35916138, seam 0.01412964, DC 0.00017037. SHA-256 звука: `d54ec660945352d0b11ade9c366697371c5068d71670aac7d2f537aef7acbf76`. Это **хэш звука**, не установочного JAR; другой encoder может дать другие байты.
- Добавлены 16 обязательных JUnit-методов и строгая проверка аудио в готовом JAR; все прежние 25 методов остаются обязательными. Полная сборка новой версии запускается CI после коммита; текущий результат проверять в GitHub Actions, не выводить из старого BUILD-REPORT.json.
- Из этой среды нельзя загрузить Gradle/Fabric-зависимости напрямую (DNS/network недоступны). Исходники для локальных standalone-проверок восстановлены из прочитанного GitHub-коммита, затем сделаны точечные изменения; это не полный локальный clone.
- Minecraft, dedicated server, два клиента, прослушивание и визуальная приёмка дыма не запускались. QA остаётся открытым, PR — draft; `main`, другие модули и активные игровые профили не изменены.

## 2026-09-08 — первый парк: 0.2.0-alpha.1

- Уточнение пользователя: делать полноценный пак, не ограничиваться грузовиком. База этой итерации — `997dfdb`. Для этой предыдущей alpha.2 впоследствии подтверждены обе успешные CI-сборки ([1](https://github.com/ProkStudio/mine/actions/runs/34241542799), [2](https://github.com/ProkStudio/mine/actions/runs/34241547236)), включая 41 JUnit-метод. Исторические записи выше отражают состояние на момент их создания.
- Добавлены багги `scout_buggy` (2 места/9 слотов/1200 топлива/120 состояния) и БТР `carrier_8x8` (6/18/3200/360), с отдельными моделями, предметами, регистрациями, звуками и параметрами движения. Грузовик сохраняет старую геометрию и параметры. Это первый набор из трёх машин, не весь завершённый план.
- Введены общие профили VehicleKind, профильные физика/звук/выхлоп, 9/18/27-слотовые меню. v1 проверяет тип, диапазоны и ёмкость; подмена типа отвергается в начале restore. Общие квоты 12 размещённых машин и 8 звуковых петель не умножаются на число типов.
- Код сохранён двумя коммитами: `df9f6468b660b44f0783ce68ba1f54d3c705825c` (основа и риги), `7c77a429571c2bd35671d44bfdee7cab51967510` (полный клиент, ресурсы и регрессии). Списки изменений подтверждены: только `military-vehicles/`. Ключевые общие, клиентские, тестовые, регистрационные исходники и генераторы сверены с локальными по Git blob SHA.
- Локально выполнены чистая компиляция используемого ядра с `--release 21` и три standalone-набора: **CoreSmoke 90041**, **FeedbackSmoke 20487**, **FleetSmoke 365909 assertions**, все PASS. Синтаксически разобраны 32 локально имеющихся Java-файла; Python-скрипты и whitespace проверены. Локальная рабочая копия по-прежнему частичная, поэтому это не заявляется локальным clean Fabric build или локальным JUnit.
- Генератор моделей реально создал 5 modern item definitions/моделей, 13 PNG-текстур и fleet manifest. Геометрия: грузовик 76 частей/158 кубоидов, багги 73/140, БТР 102/217. JSON разобран; все координаты item-элементов находятся в 0..16.
- Генератор звука реально закодировал и декодировал три разные mono 32000 Hz Vorbis-петли по 128000 samples. Багги: 34668 байт, RMS 0.11959455, peak 0.41098022, seam 0.02465820, DC 0.00014676; БТР: 34755 байт, RMS 0.11976593, peak 0.36370850, seam 0.03436279, DC 0.00003789. Локальный OGG грузовика сохранил прежний SHA-256 `d54ec660945352d0b11ade9c366697371c5068d71670aac7d2f537aef7acbf76`. Это не хэши CI JAR и не прослушивание в игре.
- **Фактический offline визуальный осмотр:** по отдельности открыты все 8 итоговых PNG: front/rear каждого типа и по одному виду багги/БТР с вращением и рулением. После первых видов исправлены квадратная запаска багги и слишком большие окна БТР, виды пересозданы и просмотрены повторно. Проверены различимые силуэты, крепления, колёса/хабы и общий вид. Это рендер экспортированных кубоидов с упрощённым цветом/светом, не Minecraft, не проверка UV, пассажиров, коллизий мира или игровой анимации.
- **Полный CI для `7c77a429571c2bd35671d44bfdee7cab51967510`: обе сборки success** — [34249216371](https://github.com/ProkStudio/mine/actions/runs/34249216371) и [34249221850](https://github.com/ProkStudio/mine/actions/runs/34249221850), завершены 2026-09-08 16:10:26/16:10:40 UTC. Выполнены clean common/client build, standalone-наборы, **65 реальных JUnit cases без failures/errors/skips**, строгая проверка remapped JAR и загрузка артефактов. Verifier требует все прежние 41 и 24 новых метода; проверяет 5 предметов, 3 профиля, 3 неповторяющихся звука и содержимое JAR.
- Установочный артефакт: `military-vehicles-0.2.0-alpha.1.jar`; отчёт с фактическими размером/SHA-256 и test XML находится в артефактах этих CI. JAR отдельно в эту среду не скачивался, его локальный checksum не придумывается. Для последующих коммитов смотреть соответствующий новый CI, не переносить этот результат автоматически.
- README/ROADMAP/HANDOFF/QA/происхождение обновляются под весь парк; добавлен воспроизводимый offline-рендерер. Следующие содержательные этапы — гусеничная основа/танк, снабжение/ремонт/инженерная техника, артиллерия и игровой цикл. Игровая QA ведётся параллельно, а не заменяет расширение парка.
- Minecraft, dedicated server, несколько клиентов, реальный мир старой версии, прослушивание в игре, FPS/MSPT и совместимость с шейдерами **не проверены**. Активные миры/профили не менялись, EULA не принималась, main и другие модули не изменялись; PR остаётся draft, stable не публикуется.

## 2026-09-09 — 0.3.0-alpha.10: десантный отсек и посадка БТР

- Продолжение серии геометрических итераций alpha.6–alpha.9 (гусеничный ходовой комплект, оснащение служебных кузовов, подвеска багги и БТР, тент с лавками и задний борт грузовика). База этой итерации — вершина `0f130015` ветки `work/military-vehicles-autonomous`.
- В риг БТР `carrier_8x8` добавлены **16 деталей / 23 кубоида**: центральный проход по полу между рядами кресел, потолочные поручни с подвесами по обоим бортам, плафоны в корпусах у потолка, бортовые стеллажи снаряжения, аптечка и огнетушитель на кронштейне у кормовой перегородки, ступень за рампой с двумя кронштейнами и вертикальные поручни по краям кормового проёма. Итог по ригу: **151 деталь / 276 кубоидов**.
- Добавлен новый standalone-набор `TroopBayModelCases`/`TroopBayModelSmoke` из 5 наборов проверок: комплект есть только у БТР и не течёт в другие семь ригов; все кубоиды внутри высотного и yaw-независимого коллайдера; нет пересечений ни между собой, ни с корпусом, ни с прежним ходовым комплектом, рампой, дымовыми пусковыми и крышевой корзиной; сохранён зазор до окружностей вращения всех восьми колёс; геометрия соответствует замыслу посадки — проход ниже подушек, шире 10 единиц и накрывает все три ряда кресел, поручни выше спинок и не пробивают крышу, стеллажи внутри борта, ступень ниже кормового бампера, поручни рампы выше габаритных огней и вне проёма шириной 22.
- `tests/run-models.sh` компилирует и запускает новый набор вместе с прежними четырьмя.
- **Фактически выполнено локально** (офлайн, Corretto 25, без Gradle): `TroopBayModelSmoke` — PASS, **5894 assertions**; заново прогнанный `WheeledModelCases` — PASS, **10035 assertions**; повтор инвариантов уровня `FleetGeometryTest` для всех восьми ригов (уникальность имён, известные материалы, высотный и yaw-независимый коллайдер, неизменность донорского грузовика) — PASS.
- **Не выполнено:** Gradle `clean build`, JUnit, `python3 tools/verify_build.py`, CI, генерация моделей/текстур/звука, offline-рендер и любая проверка в Minecraft. Число обязательных JUnit-методов (133) не менялось; в верификаторе нет счётчика standalone-наборов, поэтому gate не ослаблялся и не переписывался.
- README и HANDOFF сохраняют формулировки уровня alpha.4 (в том числе «семь standalone-наборов») и в этой итерации намеренно не переписывались, чтобы не выдавать непроверенные утверждения за проверенные. Save v1, registry ID, вместимость, места, звук, физика и баланс не тронуты; `main`, Harvester и Military Arsenal не изменялись, PR остаётся draft.

## 2026-09-10 — 0.3.0-alpha.11: БМП «Лансер», девятая машина парка

- Уточнение пользователя: добавлять именно боевую технику, а не вторичную. Поэтому следующим шагом после десантного отсека БТР стала не служебная машина, а гусеничная боевая машина пехоты. База этой итерации — вершина `db799573` ветки `work/military-vehicles-autonomous`.
- Добавлен девятый профиль `lancer_ifv`: **6 мест, 18 грузовых слотов, 2600 топлива, 300 состояния, 12 катков, 0 управляемых колёс**. Ход быстрее танкового, прочность ниже танковой: машина берёт мобильность и десант, но не выдерживает танковый бой. Профиль числится гусеничным и вооружённым; звук переиспользует танковый двигатель, поэтому набор звуковых петель остался из пяти.
- Правила огня заданы отдельно от танковых: автопушка бьёт на **40 блоков** вместо 64, урон **9** вместо танкового, углы наведения **от −45° до +15°** вместо танковых. Перезарядка равна танковой; серверные лимиты, запрет урона игрокам и блокам и правило «только враждебные мобы» не менялись.
- Собран собственный риг: **195 деталей / 297 кубоидов**. Гусеничный ход — по 18 траков и 6 катков на борт с зубчатыми венцами, направляющими колёсами, натяжителями, поддерживающими роликами, грязесъёмниками, экранами и запасными траками. Корпус — ступенчатый лоб, люк, приборы и рычаги водителя, буксирные проушины, светотехника. Десантный отсек — кормовая рампа с петлями, ручкой и ступенью, крышевой люк, поручни, плафоны и четыре бойницы в рамках. Башня — автопушка с маской, дульным тормозом и спаренным пулемётом, ПТУР-модуль, дымовые пусковые, оптика с козырьком, корзина и антенна. Выхлоп вынесен вперёд-влево, потому что корма занята рампой.
- **Фактически выполнено локально** (офлайн, Corretto 25, без Gradle): standalone-проверка `LocalIfv` — PASS, **1653738 проверок** по всем девяти ригам. Проверены уникальность имён, только известные материалы палитры, попадание всех кубоидов в высотный и yaw-независимый коллайдер, отсутствие пересечений новых узлов между собой, с корпусом и с ходовой частью, зазоры до окружностей вращения катков, соответствие посадочных мест профилю и неизменность донорского грузовика. По ходу проверки найдена и исправлена ошибка самого верификатора: он применял правила круглых колёс к легаси-грузовику.
- Обновлены инварианты парка: `OperationsCases` и `FleetProfileTest` требуют девять профилей, три гусеничных и три служебных, собственное «миссионное» оборудование у каждого нового рига, четыре бойницы и рампу у БМП, а также ограничение угла и темпа наведения уже для трёх вооружённых машин. Набор JUnit-методов не менялся, поэтому обязательные **133 case** в `tools/verify_build.py` остались теми же.
- Гейт сборки, реестр ресурсов, рецепт и локализация подняты до девяти машин: в `expected_fleet` добавлен кортеж БМП, размер манифеста стал 9, набор гусеничных — из трёх машин, крафт получил свой опознавательный модуль (арбалет), в RU/EN добавлена тройка ключей `item./entity./help.` с сохранением паритета ключей и числа `%s`. Версия пакета поднята до `0.3.0-alpha.11`.
- Код и данные разложены пятью коммитами: `74472e78` (профиль, геометрия, правила огня), `d84ad444` (тесты парка), `844bf61f` (ресурсы, рецепт, версия), `ecda909e` (гейт сборки), `760ce7da` (локализация).
- **Не выполнено:** Gradle `clean build`, JUnit, `python3 tools/verify_build.py`, CI, генерация моделей/текстур/звука, offline-рендер и любая проверка в Minecraft. До запуска генератора ресурсов у БМП нет модели, текстур и иконки предмета, поэтому проверки предметов и манифеста упадут на отсутствии файлов, а не на логике — это ожидаемое состояние ветки, а не пройденный gate.
- README, ROADMAP и HANDOFF по-прежнему описывают состояние уровня alpha.4 (восемь машин, «семь standalone-наборов») и намеренно не переписывались, чтобы не выдавать непроверенное за проверенное. Save v1, registry ID прежних восьми машин, вместимость, места, звук, физика и баланс не тронуты; `main`, Harvester и Military Arsenal не изменялись, PR остаётся draft.
